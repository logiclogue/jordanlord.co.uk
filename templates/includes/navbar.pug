.navbar
    .ten.columns.offset-by-one
        a#a-title= sitename
        ul
            for item in navbar
                if item.path === rootPath(path)
                    li.active: a(href=`/${item.path}`)= item.title
                else
                    li: a(href=`/${item.path}`)= item.title
